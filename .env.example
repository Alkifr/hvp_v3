DATABASE_URL="postgresql://hangar:hangar@localhost:5432/hangar_planning?schema=public"
DATABASE_CLOUD_URL="postgresql://user:password@host:6432/hangar_planning?schema=public"
TZ="Europe/Moscow"
# Разработка: CORS на Vite. Стенд в контуре (npm start): оставьте пустым.
CORS_ORIGINS="http://localhost:3000"
JWT_SECRET="change_me_dev_secret_use_24plus_in_production"
NODE_ENV="development"
# Стенд по HTTP без TLS (закрытый контур, прямой :3000):
# NODE_ENV=production
# COOKIE_SECURE=0
# CORS_ORIGINS=
# HTTPS за nginx (https://hvp.atechnics.ru): HOST=127.0.0.1, COOKIE_SECURE не ставить в 0
# PORT не задавайте: npm start → :3000, npm run dev → API :3001
